{{ if .is_desktop -}}
# Machine-specific Hyprland config (not managed by dotfiles)
# Add monitor configuration, device-specific settings, etc.
#
# Example monitor config:
# monitor = eDP-1, 1920x1080@60, 0x0, 1
{{ end -}}
