# mpv configuration

# Auto-loop all files
loop-file=inf

# Show estimated frame number in the OSD status line (seek, `o`, frame-step)
osd-fractions
osd-status-msg='${time-pos} / ${duration} (${percent-pos}%)  frame ${estimated-frame-number} / ${estimated-frame-count}'
