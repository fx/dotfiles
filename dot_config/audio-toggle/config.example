# Audio toggle configuration (machine-specific)
# Copy this file to 'config' and fill in your device names
#
# Find sink names: pactl list sinks short
# Find card/profiles: pactl list cards | grep -E "(Name:|profile|hdmi)" | head -40

AUDIO_SINK_SPEAKERS=""
AUDIO_SINK_HEADPHONES=""

# Optional: For HDMI outputs that require card profile switching
# AUDIO_CARD=""
# AUDIO_PROFILE=""
